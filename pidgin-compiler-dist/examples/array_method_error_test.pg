// Test method errors
let fixed = [1, 2, 3];
let dynamic = {10, 20, 30};

// This should work
print "Dynamic array: {}", dynamic;
dynamic = dynamic.push(40);
print "After push: {}", dynamic;

// This should cause an error - push on fixed array
print "Trying to push to fixed array...";
fixed = fixed.push(50);
print "This should not print: {}", fixed; 